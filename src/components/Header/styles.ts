import Styled from 'styled-components';

import { BOX_SHADOW, SPACING } from 'Src/styles/theme';

export const Header = Styled.header`
  padding: ${SPACING.default} 0;
  box-shadow: ${BOX_SHADOW.default};
`;
